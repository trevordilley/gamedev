{
  "name": "games-deadlifter",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/games/deadlifter/src",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/games/deadlifter"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "games-deadlifter:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "games-deadlifter:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "games-deadlifter:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "games-deadlifter:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "games-deadlifter:build:development"
        },
        "production": {
          "buildTarget": "games-deadlifter:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../../coverage/apps/games/deadlifter"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/games/deadlifter/**/*.ts"]
      }
    }
  }
}
